# 🎓 Autonotes Frontend

<div align="center">

![React](https://img.shields.io/badge/React-19.2.0-blue?logo=react)
![React Router](https://img.shields.io/badge/React_Router-6.0+-orange?logo=reactrouter)
![Axios](https://img.shields.io/badge/Axios-HTTP_client-green)

Клиентское приложение для сервиса Autonotes.

</div>

## ✨ Особенности реализации

*   **Аутентификация**: Полная поддержка JWT (хранение в LocalStorage, интерцепторы Axios для добавления заголовка `Authorization`, авто-логаут при 401 ошибке).
*   **Real-time UX**: Дэшборд автоматически опрашивает сервер (Polling) каждые 10 секунд, если в списке есть конспекты со статусом `PROCESSING`, чтобы пользователь увидел результат без перезагрузки страницы.
*   **Валидация**: Проверка типов файлов (изображения) и размера (до 10 МБ) на клиенте перед отправкой.
*   **Защищенные маршруты**: Компонент `ProtectedRoute` не позволяет неавторизованным пользователям попасть на внутренние страницы.

## 🚀 Установка и запуск

### 1. Установка зависимостей
```bash
cd frontend
npm install
```

### 2. Настройка окружения
Создайте файл `.env` в папке `frontend`:
```properties
REACT_APP_API_BASE_URL=http://localhost:8080/api/v1
```

### 3. Запуск в режиме разработки
```bash
npm start
```
Приложение откроется на `http://localhost:3000`.

## 📡 Взаимодействие с API

Приложение использует библиотеку **Axios** для всех HTTP-запросов. Конфигурация находится в `src/api/axios.js`.

### 🔐 Глобальная конфигурация (Axios)
*   **Base URL**: Берется из переменной окружения `REACT_APP_API_BASE_URL`.
*   **Request Interceptor**: Автоматически добавляет заголовок `Authorization: Bearer <token>`, если токен есть в `localStorage`.
*   **Response Interceptor**: Если сервер возвращает `401 Unauthorized`, приложение автоматически удаляет токен и перенаправляет пользователя на `/login`.

### Endpoints

#### 1. Аутентификация (`src/services/authService.js`)

| Метод | URL | Описание | Тело запроса (JSON) | Ответ |
|:---|:---|:---|:---|:---|
| `POST` | `/auth/register` | Регистрация нового пользователя | `{ "username": "...", "email": "...", "password": "..." }` | `{ "token": "jwt..." }` |
| `POST` | `/auth/login` | Вход в систему | `{ "username": "...", "password": "..." }` | `{ "token": "jwt..." }` |

> **Примечание:** Полученный JWT токен сохраняется в `localStorage` и используется для декодирования username (из payload токена) для отображения в шапке профиля.

#### 2. Профиль (`src/services/authService.js`)

| Метод | URL | Описание | Параметры | Ответ |
|:---|:---|:---|:---|:---|
| `GET` | `/users/{username}` | Получение данных профиля | `username` извлекается из токена | `{ "id": 1, "username": "...", "email": "...", "createdAt": "..." }` |

#### 3. Конспекты (`src/services/noteService.js`)

**Получение списка (Dashboard):**
*   **GET** `/notes`
*   **Логика:** Возвращает массив объектов `NoteDto`.
*   **Polling (Авто-обновление):** В компоненте `NotesDashboard.js` реализован механизм опроса. Если в списке есть хотя бы один конспект со статусом `PROCESSING`, клиент автоматически повторяет запрос `GET /notes` каждые **10 секунд**, пока все конспекты не перейдут в конечный статус (`COMPLETED` или `FAILED`).

**Создание конспекта (Upload):**
*   **POST** `/notes`
*   **Тип:** `multipart/form-data`
*   **Валидация на клиенте:**
    *   Тип файла: только изображения (`image/*`).
    *   Размер: не более **10 МБ**.
*   **Структура запроса:**
    *   `title` (Text): Заголовок конспекта.
    *   `file` (Binary): Файл изображения.

**Детали конспекта:**
*   **GET** `/notes/{id}`
*   **Ответ (`NoteDetailDto`):**
    ```json
    {
      "id": 1,
      "title": "Лекция",
      "status": "COMPLETED",
      "recognizedText": "Полный текст...",
      "summaryText": "Краткое содержание...",
      "createdAt": "..."
    }
    ```
*   Поля `recognizedText` и `summaryText` отображаются только если статус `COMPLETED`.

**Удаление:**
*   **DELETE** `/notes/{id}`
*   Удаляет запись из БД и вызывает очистку файла в S3 на бэкенде.