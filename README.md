# 🎓 Autonotes

**Autonotes** — это интеллектуальная платформа для автоматического создания структурированных конспектов из фотографий лекционных досок.

Проект состоит из микросервисной архитектуры, включающей REST API на Java, SPA на React, очередь сообщений RabbitMQ, объектное хранилище MinIO и базу данных PostgreSQL.

---

## 🏗️ Архитектура

Проект разделен на следующие компоненты:

1.  **Frontend (`/frontend`)**: React-приложение (SPA), предоставляющее пользовательский интерфейс для загрузки фото и просмотра конспектов.
2.  **Backend (`/backend`)**: Spring Boot приложение. Выполняет роль API Gateway, управляет пользователями, сохраняет файлы в S3 (MinIO) и отправляет события на обработку.
3.  **ML Service**: (Внешний сервис) Потребляет задачи из RabbitMQ, распознает текст с изображений и генерирует конспект.
4.  **Инфраструктура**:
    *   **PostgreSQL**: Хранение метаданных (пользователи, заметки).
    *   **MinIO**: S3-совместимое хранилище для оригиналов изображений.
    *   **RabbitMQ**: Брокер сообщений для асинхронной связи между Backend и ML-воркерами.

## 🚀 Быстрый старт (Docker Compose)

Самый простой способ запустить весь стек — использовать Docker Compose.

### Предварительные требования
*   Docker и Docker Compose

### Запуск

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/NothingRedMouth/Autonotes.git autonotes
    cd autonotes
    ```

2.  Создайте файл переменных окружения `.env` в корне проекта. Вы можете скопировать пример:
    ```bash
    cp .env.example .env
    ```
    *При необходимости отредактируйте `.env` под свои нужды.*

3.  Запустите контейнеры:
    ```bash
    docker-compose up --build -d
    ```

После запуска сервисы будут доступны по адресам:
*   **Frontend**: Не включен в docker-compose по умолчанию (запускается отдельно, см. README фронтенда).
*   **Backend API**: `http://localhost:8080`
*   **Swagger UI**: `http://localhost:8080/swagger-ui.html`
*   **MinIO Console**: `http://localhost:9001`
*   **RabbitMQ Console**: `http://localhost:15672`

## 📂 Структура репозитория

*   `backend/` — Исходный код сервера (Java 24, Spring Boot 3). [Подробнее](./backend/README.md)
*   `frontend/` — Исходный код клиента (React). [Подробнее](./frontend/README.md)
*   `docker-compose.yml` — Оркестрация сервисов.

## 🧪 Разработка

Для локальной разработки отдельных частей см. инструкции в соответствующих папках.